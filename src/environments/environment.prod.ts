import { ENVIRONMENT_NAMES } from './environment-names.constant';

export const environment = {
  production: true,
  envName: ENVIRONMENT_NAMES.PROD,
  apiUrl: 'http://apiprodurl',
  apiPort: 5000
};
